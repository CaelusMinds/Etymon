## What this changes

<!-- One or two sentences. If it fixes an issue, "Fixes #123". -->

## Why

<!-- The problem, not the patch. If there was a trade-off, say what you gave up. -->

## Checklist

- [ ] Tests cover the change, including its error paths
- [ ] Public functions have XML doc comments with an `<example>`
- [ ] `dotnet fantomas src tests samples` is clean
- [ ] `CHANGELOG.md` has an entry under `## [Unreleased]`
- [ ] The build produces no new warnings

## Public API

<!-- Delete if this changes no public API. Otherwise: what was added, changed or
     removed, and whether it breaks anyone. Etymon is 0.x, so breaking changes
     are allowed -- they just need to be deliberate and written down. -->
